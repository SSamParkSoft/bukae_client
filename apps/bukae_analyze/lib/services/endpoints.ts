/**
 * API 엔드포인트 상수
 *
 * 엔드포인트 변경 시 이 파일만 수정한다.
 * Service 함수에서 직접 문자열로 쓰지 않는다.
 */
export const API_ENDPOINTS = {
  // 도메인별 엔드포인트를 여기에 추가
  // ex) channelStats: '/api/channel/stats',
  // ex) videoList: '/api/videos',
} as const
